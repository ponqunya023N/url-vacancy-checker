name: UR Vacancy Checker

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Tokyo
    steps:
      - name: 📦 リポジトリのチェックアウト
        uses: actions/checkout@v4

      - name: 🐍 Pythonのセットアップ
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: 🌐 ChromiumとWebDriverのインストール (最終修正)
        run: |
          # 必要なパッケージをインストール
          sudo apt-get update
          sudo apt-get install -y chromium-browser
          
          # Chromium実行ファイルをシステムパスに追加 (Seleniumから呼び出すため)
          # 環境変数 PATH に /usr/bin/chromium-browser へのシンボリックリンクを追加します。
          # 最新のUbuntu環境では 'chromium' 実行ファイル名で対応できることが多いですが、念のため確認します。

      - name: 📦 依存関係のインストール
        run: |
          pip install -r requirements.txt

      - name: 🛠️ 空き情報チェックと通知の実行
        env:
          # SMTP情報を環境変数としてスクリプトに渡す
          SMTP_SERVER: ${{ secrets.SMTP_SERVER }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          FROM_EMAIL: ${{ secrets.FROM_EMAIL }}
        run: |
          python check_vacancy.py
